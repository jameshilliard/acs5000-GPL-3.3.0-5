#ifndef __bootconf_h
#define __bootconf_h

/*
 *  Define those default values which may differ
 *  depending on the compiled target
 */

#define FLASH_BLOCK	(4*1024)

#define DEFAULT_TFTP_PATH	"zvmppcons.bin"
#define BOOT_ORI_FILE	"/proc/flash/boot_ori"
#define BOOT_CONF_FILE  "/proc/flash/config"

#include <uboot_def.h>

#endif	/* __bootconf_h */
